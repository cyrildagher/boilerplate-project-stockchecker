PORT=
# NODE_ENV=test
# MongoDB URI for persisting likes (use Atlas or local Mongo in production).
# When unset and NODE_ENV=test, likes are stored in memory for local functional tests.
MONGO_URI=
# Optional: secret mixed into SHA-256 when hashing client IPs (change in production)
# IP_HASH_SECRET=
